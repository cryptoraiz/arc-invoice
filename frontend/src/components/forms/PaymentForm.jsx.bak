import { useState } from 'react'
import CurrencySelect from '../ui/CurrencySelect'

export default function PaymentForm() {
  const [formData, setFormData] = useState({
    name: '',
    wallet: '',
    amount: '',
    currency: 'USDC',
    description: '',
  })
  const [generatedLink, setGeneratedLink] = useState('')

  // Helper para obter símbolo da moeda
  const getCurrencySymbol = (currency) => {
    return currency === 'EURC' ? '€' : '$'
  }

  const handleSubmit = (e) => {
    e.preventDefault()

    // Gerar link mockado (em produção seria chamada a smart contract)
    const mockLinkId = Math.random().toString(36).substring(7)
    const fullLink = `${window.location.origin}/pay/${mockLinkId}`

    setGeneratedLink(fullLink)
    console.log('Form submitted:', formData)
    console.log('Generated link:', fullLink)
  }

  const copyToClipboard = () => {
    navigator.clipboard.writeText(generatedLink)
    alert('Link copiado para área de transferência! ✅')
  }

  return (
    <div className="relative group">
      {/* Glow Effect */}
      <div className="absolute inset-0 bg-gradient-to-br from-blue-500/20 to-cyan-500/20 rounded-[2rem] blur-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>

      {/* Card */}
      <div className="relative bg-gradient-to-br from-white/[0.07] to-white/[0.02] backdrop-blur-xl border border-white/10 rounded-[2rem] p-6 shadow-2xl">
        <div className="flex items-start gap-3 mb-5">
          <span className="text-2xl">🔗</span>
          <div>
            <h2 className="text-xl font-bold">Criar Link</h2>
            <p className="text-xs text-gray-400">Configure seu pagamento</p>
          </div>
        </div>

        <form onSubmit={handleSubmit} className="space-y-4">
          {/* Nome */}
          <div className="input-group">
            <input
              type="text"
              className="input-float py-3 text-sm"
              placeholder=" "
              id="name"
              value={formData.name}
              onChange={(e) => setFormData({ ...formData, name: e.target.value })}
              required
            />
            <label htmlFor="name" className="label-float top-1/2 -translate-y-1/2 text-xs">
              Nome ou Empresa
            </label>
          </div>

          {/* Wallet */}
          <div className="input-group">
            <div className="relative">
              <span className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-500 text-sm">💼</span>
              <input
                type="text"
                className="input-float pl-10 py-3 text-sm"
                placeholder=" "
                id="wallet"
                value={formData.wallet}
                onChange={(e) => setFormData({ ...formData, wallet: e.target.value })}
                pattern="^0x[a-fA-F0-9]{40}$"
                required
              />
              <label htmlFor="wallet" className="label-float top-1/2 -translate-y-1/2 text-xs" style={{ left: '40px' }}>
                Sua Wallet de Recebimento
              </label>
            </div>
            <button
              type="button"
              onClick={async () => {
                const text = await navigator.clipboard.readText()
                setFormData({ ...formData, wallet: text })
              }}
              className="absolute right-3 top-1/2 -translate-y-1/2 px-2 py-1 rounded-lg bg-blue-500/10 text-blue-400 text-xs font-semibold hover:bg-blue-500/20 transition"
            >
              Colar
            </button>
          </div>

          {/* Valor + Moeda */}
          <div className="grid gap-3 items-start" style={{ gridTemplateColumns: '1.2fr 1fr' }}>
            {/* Valor */}
            <div className="input-group">
              <div className="relative">
                <span className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-500 text-lg">$</span>
                <input
                  type="number"
                  className="input-float pl-8 text-xl font-bold"
                  placeholder=" "
                  id="amount"
                  step="0.01"
                  min="0"
                  style={{ padding: '14px 12px 14px 32px', height: '52px' }}
                  value={formData.amount}
                  onChange={(e) => setFormData({ ...formData, amount: e.target.value })}
                  required
                />
                <label htmlFor="amount" className="label-float top-1/2 -translate-y-1/2 text-xs" style={{ left: '32px' }}>
                  Valor
                </label>
              </div>
            </div>

            {/* Moeda */}
            <div className="h-[52px]">
              <CurrencySelect
                value={formData.currency}
                onChange={(currency) => setFormData({ ...formData, currency })}
                compact={true}
              />
            </div>
          </div>

          {/* Taxa Info */}
          <div className="flex items-center gap-2 px-3 py-2 rounded-xl bg-green-500/10 border border-green-500/20">
            <span className="text-green-400 font-bold text-xs">✓</span>
            <span className="text-xs text-green-400">Taxa &lt; $0.001 • Instantâneo</span>
          </div>

          {/* Descrição */}
          <div className="input-group">
            <textarea
              className="input-float resize-none py-3 text-sm"
              rows="2"
              placeholder=" "
              id="description"
              value={formData.description}
              onChange={(e) => setFormData({ ...formData, description: e.target.value })}
            ></textarea>
            <label htmlFor="description" className="label-float top-3 text-xs">
              Descrição (opcional)
            </label>
          </div>

          {/* Botão */}
          <button
            type="submit"
            className="relative overflow-hidden gradient-button w-full px-6 py-4 rounded-xl font-bold text-lg text-white flex items-center justify-center gap-2"
          >
            {/* Shimmer effect */}
            <span className="absolute inset-0 -translate-x-full bg-gradient-to-r from-transparent via-white/30 to-transparent animate-[shimmer_3s_infinite]"></span>

            Gerar Link →
          </button>

          {/* Link Gerado */}
          {generatedLink && (
            <div className="mt-4 p-4 rounded-xl bg-green-500/10 border border-green-500/20 space-y-3">
              <div className="flex items-center gap-2">
                <span className="text-green-400 text-xl">✓</span>
                <span className="text-green-400 font-semibold text-sm">Link gerado com sucesso!</span>
              </div>

              <div className="flex gap-2">
                <a
                  href={generatedLink}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex-1 px-3 py-2 bg-slate-900/50 border border-slate-700/20 rounded-lg text-xs text-cyan-400 hover:text-cyan-300 font-mono transition-colors truncate"
                >
                  {generatedLink}
                </a>
                <button
                  type="button"
                  onClick={copyToClipboard}
                  className="px-4 py-2 bg-blue-500 hover:bg-blue-600 rounded-lg text-white text-xs font-semibold transition-colors"
                >
                  Copiar
                </button>
              </div>

              {/* Redes Sociais */}
              <div className="pt-2 border-t border-white/5">
                <p className="text-xs text-gray-400 mb-2">Compartilhar via:</p>
                <div className="flex gap-2">
                  <a
                    href={`https://wa.me/?text=${encodeURIComponent(`Pagamento via ArcPay: ${generatedLink}`)}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex-1 py-2 rounded-lg bg-[#25D366]/10 hover:bg-[#25D366]/20 border border-[#25D366]/20 text-[#25D366] text-xs font-semibold flex items-center justify-center gap-2 transition-colors"
                  >
                    <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>
                    WhatsApp
                  </a>

                  <a
                    href={`https://twitter.com/intent/tweet?text=${encodeURIComponent('Pagamento via ArcPay')}&url=${encodeURIComponent(generatedLink)}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex-1 py-2 rounded-lg bg-black/30 hover:bg-black/50 border border-white/10 text-white text-xs font-semibold flex items-center justify-center gap-2 transition-colors"
                  >
                    <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
                    X
                  </a>

                  <a
                    href={`https://t.me/share/url?url=${encodeURIComponent(generatedLink)}&text=${encodeURIComponent('Pagamento via ArcPay')}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex-1 py-2 rounded-lg bg-[#0088cc]/10 hover:bg-[#0088cc]/20 border border-[#0088cc]/20 text-[#0088cc] text-xs font-semibold flex items-center justify-center gap-2 transition-colors"
                  >
                    <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 11.944 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/></svg>
                    Telegram
                  </a>
                </div>
              </div>
            </div>
          )}
        </form>
      </div>
    </div>
  )
}
